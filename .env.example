# Copy this file to .env if you want shell reference, but never commit .env with real values.
# dbtalk reads this variable directly from the environment — no .env loading library is used.
# Set it in your shell before running dbtalk, or pass it via docker --env.
ANTHROPIC_API_KEY=sk-ant-...